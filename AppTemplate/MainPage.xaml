<Page
    x:Class="AppTemplate.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppTemplate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Width="1280"
    Height="720" Loaded="Page_Loaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <FlipView x:Name="flipView" ItemsSource="{Binding}" Grid.Row="1" SelectionChanged="flipView_SelectionChanged">
            <FlipView.ItemTemplate>
                <DataTemplate>
                    <Grid x:Name="gridContent">
                        <GridView ItemsSource="{Binding ListDatas}">
                            <GridView.ItemTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding ThumbName}" Stretch="Fill" Width="150" Height="120"></Image>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </Grid>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
    </Grid>
</Page>
